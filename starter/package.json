{
  "name": "cf-temp-vs-002",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx wrangler dev",
    "build": "npm run -w ui build && esbuild src/index.ts --bundle --outdir=dist --format=esm",
    "start": "npx wrangler dev",
    "deploy": "npm run build && npx wrangler deploy",
    "deploy:migrate": "npm run build && npm run migrate:remote && npx wrangler deploy"
    "migrate:local": "npx wrangler d1 execute DB --local --file=./schema.sql --local",
    "migrate:remote": "npx wrangler d1 execute DB --file=./schema.sql --remote",
    "test": "vitest"
  },
  "dependencies": {
    "hono": "^4.6.9"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250801.0",
    "@cloudflare/vitest-pool-workers": "^0.4.5",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.4"
  },
  "workspaces": [
    "ui"
  ]
}
